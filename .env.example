# 음성 합성 서버 주소. 비어 있으면 서버 없이 데모 음성으로 동작한다.
# 값을 채우면 src/services/tts/ttsAdapter.ts 가 자동으로 실서버 호출로 전환된다.
# 예) VITE_TTS_API_BASE=https://tts.example.com
VITE_TTS_API_BASE=

# 정적 HTML 프로토타입(prototype/)에는 .env 가 없다 — config.js 의 ttsApiBase 에 같은 값을 적는다.
